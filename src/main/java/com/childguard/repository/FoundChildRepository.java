package com.childguard.repository;

import com.childguard.model.FoundChild;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FoundChildRepository extends JpaRepository<FoundChild, Long> {
}
